/** @type {import('next').NextConfig} */
const nextConfig = {
   images: {
    domains: ['axvctqpgabterzldygfj.supabase.co'], // 👈 inserisci il dominio del tuo Supabase project
  },
};

export default nextConfig;
